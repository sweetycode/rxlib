import type { VNode } from "preact";
import { useEffect, useRef } from "preact/hooks";

export function Tooltip({children}: {children: VNode<any>}) {
    const ref = useRef(null)
    useEffect(() => {
        
    }, [])
}